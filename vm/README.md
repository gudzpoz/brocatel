# Brocatel VM

How we turn our compiled chunks into actual story flows.

## Testing

To run the tests, you will need all five versions (Lua 5.1 ~ 5.4 & LuaJIT) of Lua installed,
as well as corresponding `busted` and `luacheck` packages.
